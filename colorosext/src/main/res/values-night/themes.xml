<resources>
    <style name="Theme.ColorOSExt" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:forceDarkAllowed">true</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>
        <!-- Status bar text color,true : black,false : white -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
    <style name="Theme.ColorOSExt.MD3" parent="Theme.Material3.Light.NoActionBar">
        <item name="android:forceDarkAllowed">true</item>
        <!-- Status bar color -->
        <item name="android:statusBarColor">?attr/colorOnPrimary</item>
        <!-- Status bar text color,true : black,false : white -->
        <item name="android:windowLightStatusBar">false</item>
    </style>
</resources>